---
title: Cross Paraphrastic Invariance Learning for Hallucination Detection
title_zh: 面向幻觉检测的跨释义不变性学习
authors: "Shanshan Lin, Dongsheng Hong, Sibo Ju, Chao Chen, Sihong Xie, Xiangwen Liao"
date: 2026-06-06
pdf: "https://arxiv.org/pdf/2606.08157v1"
tags: ["query:mm-hall"]
score: 9.0
evidence: 基于释义不变性的幻觉检测方法
tldr: "大型语言模型常生成与源文档不符的幻觉，现有检测方法依赖昂贵评估或大量标注。本文提出跨释义不变性学习框架CPIL，通过两阶段孪生网络最大化利用少量标注数据：第一阶段对比预训练学习释义不变且感知文档的嵌入，第二阶段训练轻量分类器。在LLM-AggreFact基准的11个任务上，仅用约1%标注数据即超越强基线，显著提升F1分数，展示了卓越的标签效率与泛化能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08157v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 843, \"height\": 519, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08157v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1648, \"height\": 769, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08157v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 796, \"height\": 351, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08157v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1835, \"height\": 419, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08157v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1831, \"height\": 445, \"label\": \"Table\"}]"
motivation: 现有幻觉检测方法成本高或需大量标注，亟需一种标签高效且不依赖大型评估器的方法。
method: CPIL构建释义正样本对和同文档异标签负样本对，进行两阶段对比预训练与轻量分类器训练。
result: "在LLM-AggreFact基准上，仅用1%标注数据即超越强基线，F1分数显著提升。"
conclusion: CPIL通过跨释义不变性学习实现标签高效的幻觉检测，为资源受限场景提供有效方案。
---

## 摘要
大型语言模型（LLM）经常生成缺乏源文档支持的幻觉。为了避免昂贵的LLM评估器流水线和现有分类器的大量标注需求，我们提出了CPIL（跨释义不变性学习），这是一个两阶段的孪生框架，能够最大化利用现有标注数据。具体而言，CPIL通过以下方式构建信息丰富的训练对：（i）为每个文档-声明示例生成释义视图作为正样本，并显式对齐其表示以强制执行对表面形式的不变性；（ii）挖掘同一文档中标签相反的样本对作为难负样本，以锐化对文档敏感的决策边界。然后，CPIL进行两阶段模型训练：第一阶段执行对比预训练，以学习一个释义不变且具有基源感知能力的嵌入空间；第二阶段附加一个轻量级分类器进行二值基源判断。在LLM-AggreFact基准（11个任务）上，CPIL仅使用约1%的标注数据就在F1分数上超越了强基线，展示了其预测优越性和标签效率。

## Abstract
Large language models (LLMs) frequently generate hallucinations, which are unsupported by a source document. To avoid costly LLM-as-evaluator pipelines and the heavy annotation demands of existing classifiers, we propose CPIL (Cross Paraphrastic Invariance Learning), a two-stage Siamese framework that maximizes the utility of existing labeled data. Concretely, CPIL constructs informative training pairs by: (i) generating paraphrastic views of each document-claim example as positives, and explicitly aligning their representations to enforce invariance to surface form; and (ii) mining same-document, opposite-label pairs as hard negatives to sharpen document-sensitive decision boundaries. Then CPIL conduct a two-stage model training: Stage 1 performs contrastive pretraining to learn a paraphrase-invariant, grounding-aware embedding space; and Stage 2 attaches a lightweight classifier for binary groundedness. On the LLM-AggreFact benchmark (11 tasks), CPIL surpasses strong baselines concerning F1 scores with only ~1% labeled data, showing its prediction superiority and label efficiency.

---

## 论文详细总结（自动生成）

# 面向幻觉检测的跨释义不变性学习（CPIL）——详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

大型语言模型常生成与源文档不符的“幻觉”内容，这严重阻碍了可信AI的部署。现有检测方法分为两类：(i) LLM作为评估器（如SelfCheckGPT），推理成本高昂；(ii) 事实性分类器（如SummaC、MiniCheck、FactCG），虽然部署轻量，但高度依赖大量人工标注或LLM生成的合成数据，成本仍然很高。作者指出，许多高性能检测器需要数万至百万量级的标注/合成样本，而人工或LLM标注的获取代价巨大。因此，核心问题是如何在极少量标注数据下实现高效的幻觉检测，避免额外的标注开销。

## 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：CPIL（Cross Paraphrastic Invariance Learning）是一个两阶段孪生网络框架，通过构建信息丰富的训练对，最大限度利用已有的少量标注数据。关键创新在于：(i) 生成标签保持的跨释义正样本对，并显式对齐表示以实现对表层形式的不变性；(ii) 挖掘同文档、异标签的难负样本，增强对文档敏感的决策边界。

**关键技术细节**：
- **对构建（Pair Construction）**：
  - 对每个标注样本使用回译（back-translation）生成三个释义视图（法语、西班牙语、中文作为枢轴语言），保持原标签。
  - **正样本对**：同一实例的不同释义视图（包括原始视图）组成 \( D^+ \)。
  - **负样本对**：相同文档但标签相反的样本（可选替换为释义视图）组成 \( D^- \)，形成难负样本。
- **两阶段训练**：
  - **Stage 1：孪生对比预训练**。使用权重共享的编码器 \( f_\theta \)（DeBERTa）处理配对输入，最小化边际对比损失（margin-based contrastive loss）：\( \mathcal{L}_{ctr} = \mathbb{E}[\max(0, d^+ - d^- + \alpha)] \)，其中 \( \alpha=1.5 \)。目标是将正样本对拉近、负样本对推远。
  - **Stage 2：分类器微调**。在预训练编码器上附加轻量MLP分类器 \( g_\phi \)，用少量标注集 \( D_s \) 优化二分类交叉熵损失。推理时只需单次前向传播。

公式流程：输入 \((d,c)\) → 编码器得到嵌入 \( e \) → 分类器输出逻辑值 → sigmoid得到概率。

## 3. 实验设计

- **数据集/基准**：LLM-AggreFact，一个包含11个事实性数据集的综合基准（如AggreFact、TofuEval、WiCE、REVEAL、ClaimVerify、FactExpert、LFQA、RAGTruth、CNN、XSum、MediaSum、MeetB）。官方划分和评估协议。
- **对比方法**：四种非LLM检测器：SummaC（ZS和CV变体）、AlignScore、MiniCheck、FactCG。均使用作者发布的实现和推荐检查点。
- **评价指标**：每个任务的F1分数（%），以及宏平均F1（AVG）。
- **配置**：编码器为DeBERTa，分类器为单层MLP。学习率 \(1\times10^{-5}\)。标注预算：CPIL-1%使用约1%标注（约4k对），CPIL-2%使用约2%标注（约7k对）。

## 4. 资源与算力

论文未明确说明使用的GPU型号、数量、训练时长等具体算力信息。仅提及优化设置遵循FactCG，学习率为1e-5。因此，无法量化训练开销，但可推断由于使用DeBERTa（中等规模预训练模型），且训练对数量仅数千，总体算力需求不高。

## 5. 实验数量与充分性

- **主实验**：在11个任务上报告F1，并与4个强基线全面对比（表1）。CPIL-1%和CPIL-2%均优于所有基线，且数据量仅1-2%。
- **敏感性分析**：调整标注比例从0.2%到3.2%（图3），展示不同预算下的性能曲线，验证标签效率。
- **消融实验**（表2）：
  - **数据利用策略**：对比ClsOnly（仅分类器）、Append（直接拼接释义）、RandPair（随机配对）。CPIL的配对策略全面胜出。
  - **枢轴语言选择**：单枢轴（es/zh/fr）与多枢轴对比。多枢轴效果最佳，中文枢轴单用也强。
- 实验充分：涵盖不同数据集、不同预算、不同配对策略和不同释义来源，消融设计合理，可有力支撑结论。公平性方面：基线与CPIL使用相同基础模型（DeBERTa）和类似优化设置，且基线使用了完整或更大训练集，对比客观。

## 6. 论文的主要结论与发现

- CPIL在仅使用约1%标注数据的情况下，在LLM-AggreFact的11个任务上宏平均F1达79.14%，超越所有基线（FactCG 77.14%、MiniCheck 76.47%、AlignScore 74.23%等），展示了卓越的标签效率。
- CPIL在CNN和MeetB等任务上提升最大，得益于释义不变性和文档敏感对比。
- 低预算下（0.2%标注）即可接近甚至超越全数据训练的基线，性能在1.2%左右达到平台期，说明额外增益需要更大模型或更多样的观点。
- 消融证明：跨释义正样本对和同文档难负样本对是关键；多枢轴回译优于单枢轴。

## 7. 优点

- **标签效率极高**：仅需原始数据1-2%的标注即达到SOTA，显著降低人工或LLM标注成本。
- **无需额外LLM辅助**：纯使用回译（免费翻译API）和对比学习，推理仅需单次前向传播，成本低廉。
- **方法简洁有效**：两阶段框架清晰，配对策略针对幻觉检测任务特点设计，对比损失简单但有效。
- **泛化性强**：在风格多变的摘要、对话、多源事实性检测等任务上均表现优异。
- **可扩展性**：配对构建与具体释义生成方式无关，可灵活替换为其他增强手段。

## 8. 不足与局限

- **回译质量依赖**：回译可能引入语义漂移或错误翻译，影响正样本对的保真度。论文提到未来需用蕴含过滤器等防语义漂移。
- **中等预算下的性能平台**：当标注比例超过1.2%后，F1提升有限甚至微降，表明现有容量或配对策略已进入饱和，需要更大模型或更难的负样本挖掘。
- **未覆盖多句、多跳、多模态场景**：实验仅针对单文档-单声明对的二分类，扩展到多句声明、多跳上下文或跨模态接地是未来方向。
- **实验资源细节缺失**：未报告具体GPU/时长，不利于复现和成本估算。
- **基线对比范围有限**：仅对比了非LLM分类器，未与当前更强的基于LLM的检测方法（如SelfCheckGPT、G-Eval等）直接比较（尽管作者指出这些方法推理成本高）。
- **配对生成依赖标注集大小**：若原始标签集极小（如低于0.2%），回译生成的对数有限，性能可能骤降（图3显示0.2%时F1约74-75，虽已接近FactCG但仍低于CPIL-1%）。

（完）
