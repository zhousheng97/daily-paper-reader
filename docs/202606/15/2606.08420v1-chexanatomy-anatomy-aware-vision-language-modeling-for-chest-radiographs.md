---
title: "CheXanatomy: Anatomy-Aware Vision-Language Modeling for Chest Radiographs"
title_zh: CheXanatomy：面向胸部X光片的解剖感知视觉语言建模
authors: "Sergios Gatidis, Curtis Langlotz, Christian Bluethgen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08420v1"
tags: ["query:video-med-qa"]
score: 8.0
evidence: 面向胸部X光片的医学视觉语言模型与解剖接地
tldr: 现有视觉语言模型擅长图像级理解，但缺乏细粒度解剖结构编码，难以应对分割等空间精确任务。本文提出CheXanatomy框架，通过自回归下一个token预测训练模型生成解剖分割掩码，并利用CT体数据合成逼真X光片及对应掩码实现可扩展监督。在合成和真实X光片上，该方法性能与专用U-Net相当，且对域转移具有更强的几何鲁棒性；解剖预训练还提高了有限监督下定位任务的样本效率。研究表明，将解剖结构直接嵌入生成目标可促进空间接地表示，为医学视觉语言建模提供新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08420v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 825, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1245, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08420v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1247, \"height\": 856, \"label\": \"Figure\"}]"
motivation: 现有VLM难以编码细粒度解剖结构，限制其在分割等空间精确任务中的应用。
method: 通过自回归token预测训练VLM生成解剖分割掩码，并用CT体数据合成配对的X光片与2D掩码实现监督。
result: 在分布内性能与U-Net相当，域转移下几何鲁棒性更强，且解剖预训练提升下游定位的样本效率。
conclusion: 将解剖结构嵌入生成目标可促进空间接地表示，支持解剖感知的医学VL建模。
---

## 摘要
视觉语言模型（VLM）在大规模图像-文本对上进行预训练，展现出强大的图像级理解能力，但主要针对全局对齐进行优化，并未明确编码细粒度的解剖结构，这限制了其在空间精确任务（如分割）中的适用性。我们提出了CheXanatomy，一个通过自回归令牌空间监督将显式解剖知识整合到预训练VLM中的框架。该模型无需添加特定任务解码头，而是通过下一个令牌预测来训练生成解剖分割掩码。为了实现可扩展的监督，我们从CT体积合成逼真的胸部X光片，并前向投影CT分割标签，以获得解剖上一致的2D掩码。我们在合成和真实胸部X光片上与U-Net基线进行了评估，包括对模型规模、输入分辨率和视觉编码器微调的消融研究。自回归解剖监督在分布内实现了与专用卷积模型相当的性能，并在域转移到真实CXR数据时表现出更强的几何鲁棒性。此外，在有限监督下适应新型定位任务时，解剖预训练模型展现出更高的样本效率。更大的模型和更高的输入图像分辨率提升了性能，而视觉编码器微调效果有限。这些结果表明，将解剖结构直接嵌入生成目标中促进了空间表征的建立，并支持解剖感知的医学视觉语言建模。

## Abstract
Vision-language models (VLMs) pretrained on large-scale image-text pairs demonstrate strong image-level understanding, but are primarily optimized for global alignment and do not explicitly encode fine-grained anatomical structure, limiting their suitability for spatially precise tasks such as segmentation. We introduce CheXanatomy, a framework that integrates explicit anatomical knowledge into a pretrained VLM through autoregressive token-space supervision. Instead of adding task-specific decoder heads, the model is trained to generate anatomical segmentation masks via next-token prediction. To enable scalable supervision, we synthesize realistic chest radiographs from CT volumes and forward-project CT segmentation labels to obtain anatomically consistent 2D masks. We evaluate the approach on synthetic and real chest radiographs against a U-Net baseline, including ablations on model scale, input resolution, and vision encoder fine-tuning. Autoregressive anatomical supervision achieves performance comparable to specialized convolutional models in-distribution and demonstrates improved geometric robustness under domain shift to real CXR data. In addition, anatomy-pretrained models exhibit improved sample efficiency when adapting to novel localization tasks under limited supervision. Larger models and higher input image resolution improve performance, while vision encoder fine-tuning has limited effect. These results show that embedding anatomical structure directly into the generative objective promotes spatially grounded representations and supports anatomy-aware medical vision-language modeling.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的视觉语言模型（VLM）在大规模图像-文本对上预训练后，虽然具备强大的图像级语义理解能力，但其训练目标主要面向全局对齐（如图像与文本的整体匹配），**没有显式编码细粒度的解剖结构**。这导致VLM在需要空间精确定位的任务（如解剖分割）上表现不足。
- **研究背景**：
  - 传统的卷积模型（如U-Net）在分布内分割任务上表现优异，但依赖密集像素级标注，且对域偏移（domain shift）敏感。
  - VLM（如CLIP、PaliGemma）通过全局对比学习获得可迁移的语义表征，但缺乏空间接地能力。
  - 已有工作尝试通过CT投影生成合成X光片及其标签，或使用提示分割模型（如SAM），但尚未将解剖知识系统性地注入VLM的生成目标中。
- **整体含义**：本文提出了一种将结构化解剖知识直接嵌入VLM自回归训练范式的方法，旨在同时保留VLM的语义泛化能力并赋予其空间精确性，**促进医学视觉语言模型向“解剖感知”方向发展**，从而支持更可靠、更稳健的医学影像分析。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：
  > 将解剖分割任务重新定义为**自回归下一个token预测**问题，在VLM的原始生成目标（交叉熵损失）内完成训练，**不引入任何像素级解码器或辅助分割损失**。通过大量CT导出的合成X光片及对应2D掩码，为VLM提供可扩展的解剖监督。

- **关键技术细节**：
  1. **基础模型**：使用 **PaliGemma 2** 架构（SigLIP-So400m视觉编码器 + Gemma 2大语言模型），该模型原生支持通过结构化token输出边界框和分割掩码。
  2. **掩码表示**：使用**VQ-VAE**将二值分割掩码编码为16个离散的掩码token（对应16维潜在编码）。模型在训练时预测这些离散token，推理时由VQ-VAE解码器重建为分割掩码。
  3. **合成数据生成**：
     - 从**CT-RATE数据集**获取非增强CT体积（12,984训练、683测试）。
     - 利用 **Diff-DRR** 可微分渲染器生成前后位和侧位两种投影方向的合成X光片（共25,968训练、1,366测试）。
     - 使用 **TotalSegmentator** 从CT获取3D多结构分割标签，并通过前向投影得到与合成X光片对齐的2D掩码。
     - 覆盖 **80个解剖目标**（胸廓骨骼、肺、心脏、大血管、上腹部器官等）。
     - 对合成图像施加**随机缩放**以模拟真实获取中的尺寸和几何变异。
  4. **训练策略**：
     - 采用**LoRA**（低秩适应）进行参数高效微调，大部分预训练权重冻结。
     - 两种配置：视觉编码器冻结 vs 更新。
     - 模型规模：**3B和10B参数**；输入分辨率：**224×224和448×448**。
  5. **基线**：训练标准**2D U-Net**（4层编码器-解码器，每级特征通道倍增，80个输出通道）在同一合成数据上，作为专用分割模型的对比基准。

## 3. 实验设计：数据集、场景、benchmark与对比方法

- **评估数据集**（三个）：
  1. **合成CT-RATE测试集**：1,366张前后位+侧位合成X光片（分布内评估）。
  2. **CheXmask**：200张真实前后位X光片，提供心/肺掩码（域偏移评估）。
  3. **VinDr-RibCXR**：198张真实前后位X光片，提供双侧肋骨分割（域偏移评估）。

- **基准与对比方法**：
  - **U-Net基线**：在相同合成数据上训练，输入分辨率448×448，作为卷积专用模型的代表。
  - **PaliGemma多配置**：3B/10B模型，224/448分辨率，视觉编码器冻结/微调。
  - **消融变量**：模型规模、输入分辨率、视觉编码器是否微调。
  - **下游定位任务**：利用 **RadVLM视觉定位数据集**（118,360训练、22,972测试）进行少样本迁移实验，评估解剖预训练对边界框定位样本效率的影响。

- **评估指标**：
  - 分割：Dice系数、IoU、**Hausdorff距离**、**Fourier描述子距离**（FDD）。
  - 定位：边界框IoU。

- **实验充分性**：
  - 完整的分布内+域偏移评估。
  - 多维度消融（模型尺寸×分辨率×视觉编码器行为）。
  - 少样本迁移实验覆盖0%–100%训练数据分数。
  - 定性示例展示代表性案例。
  - 总体上实验设计全面，对比公平（U-Net在相同数据上训练，指标一致）。

## 4. 资源与算力

- **明确说明**：
  - 训练在 **8块H100 GPU** 上进行。
  - 批次大小根据配置调整：10B参数模型、448分辨率时为**8**；3B参数模型、224分辨率时为**24**。
  - **未明确说明训练总时长或具体epoch数**。

## 5. 实验数量与充分性

- **实验组数量**：
  1. 分布内合成数据评估（1,366张，对比U-Net及多种VLM配置）。
  2. 域偏移真实数据评估（CheXmask + VinDr-RibCXR共398张，同样对比所有配置）。
  3. 消融实验（模型规模、分辨率、视觉编码器更新与否）。
  4. 少样本迁移实验（RadVLM，训练数据分数0%、1%、3%、10%、…、100%，共至少6个分数点）。
  5. 可视化定性比较。
- **充分性评价**：
  - **充分**：覆盖了分布内和域外场景，消融关键变量，评估了实际应用中的少样本迁移能力。
  - **公平**：U-Net和VLM使用相同训练数据，评估指标一致，未引入不公平的预处理或额外数据。
  - **客观**：报告了均值和分布（如图2箱线图），指标涵盖重叠度、边界误差和形状一致性。

## 6. 论文的主要结论与发现

1. **性能可比**：在合成数据上，最佳PaliGemma（10B, 448分辨率）的Dice/IoU接近U-Net，且**Hausdorff距离和FDD更低**，表明边界和形状更优。
2. **域偏移鲁棒性更强**：在真实X光片上，PaliGemma保持与U-Net相当的Dice/IoU，但**Hausdorff和FDD显著更低**，显示出更好的几何稳定性。
3. **规模扩展有效**：增大模型（3B→10B）和增加输入分辨率（224→448）均一致提升性能。
4. **视觉编码器微调作用有限**：冻结或更新视觉编码器对最终分割性能几乎无影响，提示解剖推理主要在**多模态token空间**产生。
5. **解剖预训练提高样本效率**：在少样本定位任务（0–3%数据）中，解剖预训练模型始终优于基线；随数据增加差距缩小。
6. **核心发现**：“将解剖结构直接嵌入生成目标可促进空间接地表示”，为医学VLM构建提供了新范式。

## 7. 优点

- **方法创新**：利用自回归token预测实现解剖分割，无需额外解码器头，完美融入VLM原生训练目标，保持了模型的语言接口和多任务灵活性。
- **数据可扩展性**：通过CT投影生成大量合成配对数据（25,968张+80结构），克服了真实CXR注释稀缺的瓶颈。
- **参数高效**：使用LoRA微调，只需更新少量参数，节省算力并保留预训练知识。
- **鲁棒性验证**：在域偏移场景下表现出明显更强的几何鲁棒性，这是传统分割网络的弱点。
- **迁移能力**：解剖先验有助于下游少样本定位任务，具有实际应用价值。

## 8. 不足与局限

- **实验局限性**：
  - 合成数据和真实数据之间存在域间隙（虽未量化但已有改善），模型在真实数据上的绝对性能仍有提升空间。
  - 评估数据集较小（CheXmask仅200张，VinDr-RibCXR仅198张），统计显著性可能受限。
  - 未在大型公开CXR分割基准（如JSRT、Japanese Society of Radiological Technology数据集）上进行评估。
  - 少样本实验仅在边界框定位任务上进行，未扩展到完整分割迁移。
- **偏差风险**：
  - 合成数据来源于单一CT数据源（CT-RATE），可能引入采集协议、患者队列偏差。
  - TotalSegmentator标注的解剖结构有限（80个），遗漏部分软组织或罕见异常结构。
- **应用限制**：
  - 目前仅对胸部X光片有效，推广到其他解剖部位（如腹部、骨骼）需要额外CT数据。
  - 模型在生成掩码时使用VQ-VAE离散编码，可能限制高细节分割精度。
  - 未与现有多模态分割方法（如SAM医学版、CLIP-Seg）进行直接对比，仅与U-Net比较。
  - 推理速度、模型部署成本等实际因素未讨论。

（完）
