---
title: Object Tokens as a Bridge Between Segmentation and Visual Question Answering in Robotic Surgery
title_zh: 对象标记作为机器人手术中分割与视觉问答的桥梁
authors: "Yiping Li, Ronald de Jong, Romy van Jaarsveld, Franco Badaloni, Gino Kuiper, Jelle Ruurda, Josien Pluim, Marcel Breeuwer"
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15861v1"
tags: ["query:video-med-qa"]
score: 9.0
evidence: 机器人手术中的视觉问答与像素级分割
tldr: 手术视觉问答(VQA)需精细理解场景，但现有模型仅用边界框等粗粒度视觉定位。本文提出统一框架，将VLM与SAM解码器结合，通过VLM生成物体token引导答案预测，并投影至SAM产生分割掩码，联合优化分割与QA目标。在RAMIE和EndoVis18数据集上，该方法显著提升VQA性能。结果表明，上下文感知物体token能增强视觉推理与像素级空间接地，推动手术场景理解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15861v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1207, \"height\": 584, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15861v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1270, \"height\": 480, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15861v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1239, \"height\": 1335, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15861v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 226, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15861v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 201, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15861v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1267, \"height\": 247, \"label\": \"Table\"}]"
motivation: 现有方法依赖边界框等粗粒度视觉定位，缺乏像素级空间结构，限制细粒度手术场景理解。
method: 集成VLM与SAM解码器，用VLM生成物体token引导答案预测，并投影至SAM产生分割掩码，联合优化分割与QA目标。
result: 在RAMIE和EndoVis18数据集上，该方法优于基线，验证了物体token对细粒度理解的提升。
conclusion: 上下文感知物体token增强了VLM的视觉推理与空间接地能力，促进手术场景理解。
---

## 摘要
机器人手术中的视觉问答（VQA），即手术VQA，需要高层次的复杂手术场景理解以及视觉感知与语言推理的整合，具有支持手术培训和术中决策的潜力。最近的视觉语言模型（VLM）通过参数高效微调显示出有希望的性能；然而，大多数现有方法依赖粗粒度的视觉定位，通常仅限于边界框，无法捕捉手术对象的细粒度空间结构。在这项工作中，我们提出了一个统一框架，在单个框架内联合执行像素级分割和视觉问答。我们的方法将VLM与基于Segment Anything模型（SAM）的解码器集成，并将场景元素表示为VLM生成的对象标记。这些对象标记指导答案预测，并进一步投影到基于SAM的解码器以生成分割掩码。通过分割和问答目标优化对象标记嵌入，模型学习了增强视觉推理的空间基础表示，同时提供明确的像素级基础。我们在私有的RAMIE（机器人辅助微创食管切除术）数据集和公共的EndoVis18数据集上评估了所提出的方法，该方法在手术VQA上持续优于基线方法。这些结果表明，将上下文感知的对象标记纳入视觉语言模型可以改善细粒度的手术场景理解。

## Abstract
Visual Question Answering (VQA) in robotic surgery, referred to as surgical VQA, requires high-level understanding of complex surgical scenes and the integration of visual perception with language reasoning, with the potential to support surgical training and intraoperative decision-making. Recent Vision-Language Models (VLMs) have shown promising performance through parameter-efficient fine-tuning; however, most existing approaches rely on coarse visual grounding, typically limited to bounding boxes, which fails to capture the fine-grained spatial structure of surgical objects. In this work, we propose a unified framework that jointly performs pixel-level segmentation and visual question answering within a single framework. Our approach integrates a VLM with a Segment Anything Model (SAM)-based decoder and represents scene elements as object tokens generated by the VLM. These object tokens guide answer prediction and are further projected to the SAM-based decoder to produce segmentation masks. By optimizing the object token embeddings through both segmentation and question answering objectives, the model learns spatially grounded representations that enhance visual reasoning while providing explicit pixel-level grounding. We evaluate the proposed method on the private RAMIE (Robot-Assisted Minimally Invasive Esophagectomy) dataset and the public EndoVis18 dataset, where it consistently outperforms baseline methods for surgical VQA. These results demonstrate that incorporating context-aware object tokens into vision-language models improves fine-grained surgical scene understanding.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：机器人手术中的视觉问答（surgical VQA）需要高层次的理解复杂手术场景，并整合视觉感知与语言推理，以支持手术培训和术中决策。然而，现有大多数方法仅依赖粗粒度的视觉定位（如边界框），无法捕捉手术对象的细粒度空间结构，限制了场景理解的精确性。
- **整体含义**：本文旨在通过引入**对象标记（object tokens）**作为桥梁，将**像素级分割**与**视觉问答**统一到一个框架中，从而提供更精细的空间接地（spatial grounding），提升视觉推理能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将场景中的每个分割目标表示为一个离散的**对象标记**（特殊token `<sam_pad>`），该标记由视觉语言模型（VLM）生成，既用于指导答案预测，又可投影到Segment Anything Model（SAM）解码器生成分割掩码。对象标记嵌入通过分割和问答两个目标联合优化，学习空间基底的表示。
- **关键技术细节**：
  - **VLM基础**：采用 **Qwen2.5-VL-7B** 作为基础VLM，使用LoRA（rank=16，α=32）进行参数高效微调。
  - **连接VLM与SAM**：将VLM输出的`<sam_pad>`特征通过线性层映射到SAM提示嵌入空间，然后通过交叉注意力层与一组可学习查询交互，得到投影的提示token。之后用SAM解码器（支持SAM、SAM2、SAM3三个变体）生成掩码。
  - **训练策略与课程学习**：采用三阶段课程学习：① 纯VQA数据热身；② 加入分割监督训练；③ 混合纯VQA和分割引导VQA联合训练。VQA损失为下一个token交叉熵，分割损失为Dice损失和二元交叉熵损失。
  - **推理灵活性**：推理时分割解码可选；VLM可直接使用学习到的对象标记作为上下文线索生成答案，或投影到SAM产生掩码。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - **EndoVis18**：原为语义分割数据集，后扩增为手术VQA数据集（5类问题：单短语、接地、区域、视觉QA、详细描述）。分训练/测试集，视频划分。
  - **RAMIE**：内部收集的机器人辅助微创食管切除术视频，带有手术阶段标签和分割掩码，VQA问题包括接地、结构识别、工具识别、计数、是否、手术阶段识别等6类。
- **基准（Benchmark）**：没有明确统一的公开基准，但对比了：
  - **分割基线**：DINOv3 ViT-L + Encoder-only Mask Transformer (EoMT)。
  - **VQA基线**：仅使用基础VLM（Qwen2.5-VL-7B）+LoRA微调，无对象标记。
  - **SOTA模型**：EndoChat、LLaMA-3.2-11B、Gemma-3-12B、GLM-4V-9B等（仅在一个数据集上比较）。
- **对比方法**：本文方法本身使用三种SAM变体（SAM、SAM2、SAM3）分别与VLM结合，并与上述基线比较。

## 4. 资源与算力

- **文中未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提到训练学习率2×10⁻⁴（VLM）和1×10⁻⁵（SAM部分），训练3个epoch。未提及硬件配置。

## 5. 实验数量与充分性

- **实验数量**：
  - 在两个数据集上分别进行了**分割性能**（Dice、HD95）和**VQA性能**（多个指标：准确率、mIoU、BLEU-4、CIDEr、Rouge-L、GPT-4评分）的对比。
  - 在每个数据集上，对比了三种SAM变体与基线的结果。
  - 在EndoVis18上额外与多个SOTA模型（4个）进行了VQA比较。
  - 可视化了因果注意力图（两个例子）。
- **充分性与公平性评价**：
  - 实验覆盖了分割和VQA两个任务，并考虑了多种问题类型，比较全面。
  - 但**消融实验不足**：没有单独分析对象标记本身对VQA的贡献（如去掉分割监督仅保留对象标记），也没有分析不同投影层设计的影响。
  - 对比SOTA时仅在EndoVis18上，未在RAMIE上对比外部模型，且SOTA对比中缺乏使用相同VLM基线的公平设置（如EndoChat可能使用不同骨干）。
  - 训练协议统一但可能对SAM3不公平（官方使用多损失函数，本文仅用单一损失）。

## 6. 论文的主要结论与发现

1. **联合训练提升效果**：将对象标记同时用分割和QA目标优化，比仅用VLM基线在VQA任务上表现更好。
2. **分割质量与VQA呈正相关**：分割性能更强的SAM变体（如SAM2）通常带来更高的VQA准确性。
3. **对象标记增强视觉推理**：注意力可视化显示，答案token对语义相关的对象标记分配更高注意力，表明模型利用接地表示进行预测。
4. **统一框架可行且灵活**：该框架可适配不同VLM和SAM变体，在私有和公共数据集上均取得SOTA或竞争性结果。

## 7. 优点：方法或实验设计上的亮点

- **创新性**：首次在手术域内将像素级分割与开放文本VQA统一到单一框架，利用对象标记桥接两个任务。
- **架构通用性**：方法不依赖特定VLM或SAM版本，支持多种变体，易于扩展。
- **课程学习策略**：三阶段训练有效平衡语言建模与分割目标，避免训练不稳定。
- **注意力可视化**：提供了因果注意力图，直观展示了对象标记对答案生成的贡献，增强可解释性。
- **数据集多样性**：在公开和私有两个不同手术场景数据集上验证，增强了结论的泛化性。

## 8. 不足与局限

- **实验覆盖有限**：消融实验较少，未深入分析各组件（如投影层、可学习查询、课程学习阶段）的独立贡献。
- **公平性存疑**：与SOTA对比仅在EndoVis18上，且基线模型可能未使用相同的VLM骨干，比较不够严格。
- **数据集局限**：
  - 仅使用单帧图像，未利用视频时序信息（本文明确说明因公开数据集有限）。
  - 现有数据集推理复杂度较浅（多为简单问答），缺乏临床级复杂推理问题。
- **资源信息缺失**：未报告训练算力消耗，影响可重复性评估。
- **分割性能上限**：尽管SAM2/3性能较好，但分割精度仍低于专用分割模型（如EoMT在某些指标上更高），可能限制对细粒度结构理解的提升。
- **部署成本**：VLM（7B参数）+ SAM解码器可能导致推理延迟，未讨论实时性。

（完）
