---
title: "OpenMedQ: Broad Open Pretraining for Medical Vision-Language Models"
title_zh: OpenMedQ：面向医学视觉语言模型的广泛开放预训练
authors: "Ibrahim Gulluk, Max Van Puyvelde, Olivier Gevaert"
date: 2026-06-11
pdf: "https://arxiv.org/pdf/2606.12953v1"
tags: ["query:video-med-qa"]
score: 9.0
evidence: 医学视觉语言模型在医学VQA基准上取得SOTA
tldr: 现有医学视觉语言模型依赖私有数据或超大规模参数，可复现性差。OpenMedQ在14个全开放医学数据集（约335万样本）上预训练，涵盖病理、放射学、显微镜和临床QA。在PathVQA上达到BLEU-1 75.9，超越562B参数的Med-PaLM M；视觉编码器在8个未见分类任务上平均macro-F1 0.757，优于BiomedCLIP等。首个广泛全开源医学视觉语言预训练基线，代码与demo公开，促进可复现研究。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12953v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1560, \"height\": 1140, \"label\": \"Figure\"}]"
motivation: 现有医学VLM依赖私有数据或超大模型，缺乏可复现的全开源预训练基线。
method: 在涵盖病理、放射学、显微镜和临床QA的14个开源数据集（335万样本）上预训练视觉语言模型。
result: PathVQA BLEU-1 75.9（超越80倍大的Med-PaLM M），VQA-MED BLEU-1 64.5，视觉编码器8分类平均macro-F1 0.757。
conclusion: 开源模型与代码为医学VLM提供强基线，证明全开源预训练可达SOTA。
---

## 摘要
我们提出OpenMedQ，一个医学视觉语言模型，在迄今为止最广泛的完全开放医学混合数据集上预训练：14个数据集，总计约335万预训练样本，涵盖病理学、放射学、显微术和纯文本临床QA。OpenMedQ在PathVQA上达到了最先进的BLEU-1（75.9），超越了参数高达562B（约大80倍）的Med-PaLM M变体，并在VQA-MED上匹配了最佳报告的BLEU-1（64.5）。其视觉编码器在相同的下游配方下转移到8个未见过的医学分类基准，获得了最高的平均macro-F1（0.757），优于BiomedCLIP（0.745）、PMC-CLIP（0.745）、PubMedCLIP（0.746）和从头训练的基线（0.616）。我们发布了代码，并且交互式演示公开可用，作为社区的可复现基线。

## Abstract
We present OpenMedQ, a medical vision-language model pretrained on the broadest fully-open medical mix to date: 14 datasets totaling ~3.35M pretraining samples spanning pathology, radiology, microscopy, and text-only clinical QA. OpenMedQ reaches state-of-the-art BLEU-1 on PathVQA (75.9), beating Med-PaLM M variants up to 562B parameters (~80x larger), and matches the best reported VQA-MED BLEU-1 (64.5). Its vision encoder, transferred to 8 unseen medical classification benchmarks under an identical downstream recipe, obtains the highest average macro-F1 (0.757) among BiomedCLIP (0.745), PMC-CLIP (0.745), PubMedCLIP (0.746), and a from-scratch baseline (0.616). We release our code and an interactive demo is publicly available as a reproducible baseline for the community.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有医学视觉语言模型（VLM）多依赖少量私有或半开源数据，或依赖超大规模参数（如562B的Med-PaLM M），导致可复现性差，社区难以直接重用、检查或扩展。
- **研究动机**：构建一个**完全开源、广泛预训练**的医学VLM基线，在中等模型规模（7B参数）下达到或超越闭源/超大模型的性能，同时公开所有代码、数据配方和交互式demo，以促进可复现研究和社区发展。
- **整体含义**：证明了**开放的、多样化的预训练数据**可以在不依赖专有大规模参数的情况下成为有力的竞争杠杆，为医疗AI的开放科学提供了可落地的基线。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：采用LLaVA风格的架构，在**最广泛的完全开源医学预训练混合数据集**（14个数据集，约335万样本）上训练，通过下一词预测任务联合学习视觉与语言表征。
- **关键技术细节**：
  - **架构**：视觉编码器为ViT-base-patch16-224（使用BiomedCLIP初始化）；语言模型为LLaMA-7B（使用PMC-LLaMA初始化）；两者通过线性投影连接。图像tokens与文本tokens拼接后从左到右自回归解码。
  - **微调策略**：使用LoRA（秩r=8）进行参数高效微调，优化目标为掩码了图像tokens和前缀tokens的下一词交叉熵损失。
  - **训练配置**：所有图像resize为224×224；优化器AdamW，batch size 64，学习率5e-5，最多训练15个epoch。
  - **分类迁移**：分离预训练后的视觉编码器，附加一个线性分类头（权重矩阵 W ∈ ℝ^{2d×m}），与编码器一起在下游数据集上端到端微调100个epoch。

## 3. 实验设计：数据集、基准与对比方法

- **预训练数据集（14个开放数据集，约335万样本）**：
  - 图像-文本对（约294万对）：涵盖病理学（PathVQA）、放射学（VQA-RAD、IU-XRAY、MIMIC-CXR、ROCO、OmniMedVQA）、混合模态（Slake、PMC-OA、PMC-VQA、VQA-MED）和显微术（μ-Bench）。
  - 纯文本临床QA（约41万样本）：MedQA、MedMCQA、PubMedQA，用于保持语言能力。
- **分类迁移任务（8个未见过的Benchmark）**：
  - CXR8、MedFMC（三个子任务：chest、colon、endo）、Breast-Ultrasound、CHAOYANG、CBIS-DDSM、Mendeley-CXray。
- **开放式VQA基准**：PathVQA、VQA-MED。
- **对比方法**：
  - 分类迁移：对比BiomedCLIP、PMC-CLIP、PubMedCLIP和从头训练基线（scratch），确保下游配方完全相同，差异仅来自预训练。
  - VQA：对比Med-PaLM M（多个变体，最大562B参数）、前缀微调方法（Van Sonsbeek et al., 2023）以及2019年VQA-MED挑战最佳结果。

## 4. 资源与算力

- **训练硬件**：单张NVIDIA A100 GPU。
- **训练时长**：未明确给出总时间，但提到使用batch size 64，最多15个epoch。
- **备注**：论文未提供详细的训练耗时、GPU内存消耗或能耗数据。

## 5. 实验数量与充分性

- **实验数量**：
  - 分类迁移：8个基准，每个基准上报告macro-F1，并计算平均值。
  - VQA：2个基准（PathVQA、VQA-MED），报告BLEU-1。
  - 对比方法：5种基线（包括从头训练）。
- **充分性与公平性**：
  - 分类迁移实验中，所有方法使用**完全相同**的下游微调配方（线性头、相同epoch数等），确保性能差异仅来源于预训练方式。
  - VQA实验中对比了多种规模的方法，包括超大模型Med-PaLM M，对比范围较广。
  - **不足**：作为短论文，缺乏消融实验（如不同预训练数据组合的影响、LoRA秩选择、学习率影响等），也缺少对模型规模缩放（不同视觉/语言骨干）的探讨。实验虽然公平，但覆盖不够全面。

## 6. 论文的主要结论与发现

- **主要结论**：
  - 使用**完全开放的、多样化的预训练数据**可以使7B参数的模型在PathVQA上达到75.9 BLEU-1，超越562B参数的Med-PaLM M（72.27），并在VQA-MED上匹配最佳结果（64.5）。
  - OpenMedQ的视觉编码器在8个未见分类任务上的平均macro-F1（0.757）优于BiomedCLIP（0.745）、PMC-CLIP（0.745）、PubMedCLIP（0.746）。
  - **数据多样性是可复现的杠杆，而专有的大规模参数不是必要条件**。
- **发现**：在部分任务（如VQA-RAD、Slake）上，Med-PaLM M的大模型仍占优；在单一模态的窄域任务（如Breast-Ultrasound）上，专用编码器可能更好。

## 7. 优点：方法或实验设计上的亮点

- **完全开源**：将发布权重、数据配方和代码，并提供交互式demo（https://openmedq.streamlit.app/），为社区提供可直接复现的基线。
- **预训练数据广度**：首个涵盖病理、放射、显微、混合模态和纯文本QA的全开源医学预训练混合，规模达335万样本。
- **高效微调**：使用LoRA在单卡A100上即可完成训练，降低了算力门槛。
- **公平比较**：分类迁移实验中采用完全相同的下游配方，消除了微调差异，使预训练质量的对比更客观。
- **显著超越**：以7B参数在PathVQA上超越80倍大的Med-PaLM M，展示了小模型通过多样化数据也能取得SOTA能力。

## 8. 不足与局限

- **实验覆盖不全面**：
  - 仅测试了分类（8个）和VQA（2个）任务，缺少分割、检测等任务评估。
  - 缺少消融研究（如单一数据源、不同数据组合、LoRA秩影响等），无法确定各数据源的具体贡献。
- **指标局限性**：BLEU-1只衡量表面n-gram匹配，不能完全反映医学VQA的语义正确性；分类仅用macro-F1，可能忽视类别不平衡。
- **领域偏差与泛化**：尽管数据多样，但Breast-Ultrasound这类特定任务上表现不如专用编码器，表明通用预训练在某些窄域仍存差距。
- **模型规模**：仅使用7B语言模型，未探索更大语言骨干是否能进一步提升；也未与其它开源医学VLM（如LLaVA-Med）直接比较（论文未提供其在该设置下的结果）。
- **资源描述不完整**：未提供训练总时间、收敛曲线等细节，复现时可能需自行估算。
- **潜在风险**：预训练数据虽然开源，但可能仍包含未知偏差或噪声；模型在真实临床部署前需要更多验证。

（完）
